import 'package:flutter/material.dart';

class WeatherInfoGrid extends StatelessWidget {
  const WeatherInfoGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Weather Info Grid');
  }
}
