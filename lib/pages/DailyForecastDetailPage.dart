import 'dart:convert';
import 'package:flutter/material.dart';

class DailyForecastDetailPage extends StatelessWidget {
  const DailyForecastDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}